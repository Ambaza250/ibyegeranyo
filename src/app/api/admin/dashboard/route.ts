import { NextResponse } from 'next/server'; import { getCurrentAdmin } from '@/lib/auth'; import { getActiveUsersCount,getPendingPaymentsCount } from '@/lib/db'; import { collections,getDb } from '@/lib/firebase';
export async function GET(){try{if(!await getCurrentAdmin())return NextResponse.json({error:'Not authenticated'},{status:401});const [activeSubscribers,pendingPayments,docs]=await Promise.all([getActiveUsersCount(),getPendingPaymentsCount(),getDb().collection(collections.documentaries).count().get()]);return NextResponse.json({activeSubscribers,pendingPayments,totalDocumentaries:docs.data().count})}catch{return NextResponse.json({error:'Unable to load dashboard'},{status:500})}}
